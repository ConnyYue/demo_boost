name: flutter_boost_example
description: flutter_boost_example
version: 1.0.0+1

environment:
  sdk: ">=2.0.0-dev.68.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: 1.0.3
  image_picker: 0.8.4+1
  video_player: 2.2.3
  webview_flutter: 2.0.13

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_boost:
    git:
      url: 'https://github.com/alibaba/flutter_boost.git'
      ref: 'v3.0-preview.16'

flutter:
  uses-material-design: true

  assets:
    - images/flutter.png